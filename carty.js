class cart {
  constructor(url, links, names) {
    this.url = url;
    this.links = links;
    this.names = names;
  }
  //get url
  get getURL(){
    return url;
  }
  //get links
  get getLinks(){
    return links;
  }
  //get names
  get getNames(){
    return names;
  }
}
